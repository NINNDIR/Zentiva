# Dependencias
/node_modules

# Entornos y Secretos (¡Crítico para Firebase!)
.env
.env.local
.env*.local

# Build de Next.js
/.next/
/out/

# TypeScript Build Info
*.tsbuildinfo
next-env.d.ts # (Opcional, aunque Next.js lo regenera, suele dejarse, pero el buildinfo sí se ignora)

# Logs y Archivos del Sistema
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
.DS_Store
Thumbs.db